import { Component, OnInit, inject, signal } from '@angular/core';
import { DatePipe } from '@angular/common';
import { RouterLink } from '@angular/router';
import { StudioDataService } from '../../../core/studio-data.service';
@Component({selector:'app-studio-content-page',imports:[RouterLink,DatePipe],template:`<main class="page"><header><div><h1>Nội dung</h1><p>Video thật đã tải lên Cloudflare R2.</p></div><a routerLink="/studio/upload">+ Tải video</a></header><label class="search">Tìm video <input type="search" (input)="search.set($any($event.target).value)"/></label><div class="table"><table><thead><tr><th>Video</th><th>Chế độ</th><th>Trạng thái</th><th>Ngày tạo</th><th>Lượt xem</th><th>Tương tác</th></tr></thead><tbody>@for(v of filtered();track v.videoId){<tr><td><a [routerLink]="['/watch',v.videoId]">{{v.title}}</a><small>{{v.duration}} giây · {{v.tags.join(', ')}}</small></td><td><span [class.locked]="v.visibility==='private'">{{v.visibility}}</span></td><td>{{v.status}}</td><td>{{v.createdAt|date:'dd/MM/yyyy'}}</td><td>{{v.stats.views}}</td><td>👍 {{v.stats.likes}} · 💬 {{v.stats.comments}}</td></tr>}@empty{<tr><td colspan="6">Chưa có video.</td></tr>}</tbody></table></div></main>`,styles:[`.page{max-width:1200px;margin:auto;padding:32px 24px}header{display:flex;justify-content:space-between;align-items:center}header h1{margin:0}header p,small{color:var(--text-muted)}header a{padding:10px 15px;border-radius:9px;background:var(--primary);color:#fff;text-decoration:none}.search{display:flex;gap:10px;align-items:center;margin:22px 0;color:var(--text-muted);font-size:.8rem}.search input{padding:9px 12px;border:1px solid var(--line);border-radius:8px;background:var(--surface);color:var(--ink)}.table{overflow:auto;border:1px solid var(--line);border-radius:14px;background:var(--surface)}table{width:100%;border-collapse:collapse}th,td{padding:13px 15px;border-bottom:1px solid var(--line);text-align:left;white-space:nowrap;font-size:.82rem}th{background:var(--canvas);color:var(--text-muted)}td:first-child{display:grid;gap:4px;min-width:280px}td a{color:var(--ink);font-weight:700;text-decoration:none}.locked{color:#b45309}`]})
export class StudioContentPage implements OnInit{readonly data=inject(StudioDataService);readonly search=signal('');ngOnInit(){this.data.load()}filtered(){const q=this.search().toLowerCase();return this.data.videos().filter(v=>v.title.toLowerCase().includes(q))}}
